# Lango-Client

Lango - продвинутое приложение для изучение иностранных языков при помощи современных инструментов.

---

## 📜 Команды

- `dev` - запустить проект в режиме разработки

- `build` - собрать проект в конечный оптимизированный вариант

- `generate:slice` - сгенерировать готовые шаблоны указанного слоя (подробнее в архитектуре)

- `test:unit` - запустить проверку unit тестов

- `test:reports` - создать отчёты тестов в браузерном варианте 

- `analyzer` - запустить аналитику бандла проекта в браузере

- `storybook` - запустить документацию (storybook) компонентов в браузере

- `lint:ts` - пройтись по указанным ts и tsx файлам на форматтирование кода

- `lint:scss` - пройтись по указанным scss и css файлам на форматтирование кода



## 🏗️ Архитектура

Проект разрабатывается по архитектуре [FSD](https://feature-sliced.design/ru/docs/get-started/overview)

### Генерация слоёв

`generate:slice` - создаёт шаблон на основе заданных параметров

Полный вариант команды: `npm run generate:slice <layer> <slice>`

- **layer** - entities, features, pages
- **slice** - название любой сущности

Пример:

```TypeScript
  npm run generate:slice entities user
```

Создаст сущность `User` внутри слоя entities

**Структура папок:**


```
📂 User/
|
📂 ui/
│   └── 📂 User/
│       ├── User.tsx
│       └── User.module.scss
│   └── index.ts
|
📂 model/
    📂 types/
    │   └── UserSchema.ts
    📂 slices/
        └── UserSlice.ts

```

### Архитектурные принципы:

За всеми принципами следят eslint правила, которые при нарушении будут выдавать ошибку. Важно соблюдать эти правила

 1. **Импорт должен происходить из ниже стоящего слоя в выше стоящие**  
  Слои по уровням `shared` -> `entities` -> `features` -> `widgets` -> `pages` -> `app`  
  Запрещено импортировать в обратном или хаотичном порядке за исключением специфичных ситуаций

 2. **Внутри одного слоя нельзя импортировать компоненты между собой**

    > ⚠️ **Предупреждение:** *В entities и shared между собой компоненты импортировать можно*  
    

    **Примры:**  


    ❌ Некорректный пример - импорт осуществялется из `features`

    ```ts
      // features/AuthForm
      import { ProfileInput } from "@/features/ProfileInput"

      export const AuthForm = () => {
        return (
          <form>
            <ProfileInput />
          </form>
        )
      }
    ```

    ✅ Корректный пример - импорт осуществялется из `entities`

    ```ts
    // features/AuthForm
    import { ProfileInput } from "@/entities/ProfileInput"

    export const AuthForm = () => {
      return (
        <form>
          <ProfileInput />
        </form>
      )
    }
    ```
     За импортами следит **eslint** плагин, который не даст нарушить данное правило

 3. **При импорте компонентов внутри одного слоя путь должен быть относительным**  
    Применяется для слоя `shared` и `entites`

    **Примры:**  


    ❌ Некорректный пример - Импорт интерфейса для расширения ProfileSchema импортируется абсолютно

    ```ts
      // entities/profile/model/types/ProfileSchema.ts
     import type { User } from "@/entities/user"

     interface Profile extends User {
      description:string;
      links: string[]
     }
    ```

    ✅ Корректный пример - Импорт интерфейса для расширения ProfileSchema импортируется относительно

    ```ts
      // entities/profile/model/types/ProfileSchema.ts
     import type { User } from "../../../User/model/types/UserSchema"

     interface Profile extends User {
      description:string;
      links: string[]
     }
    ```

 4. **Только нужный код (компоненты, типы, и так далее) экспортируется из общего файла - index.ts**  
    Все компоненты, которые необходимы в других слоях отдаются наружу только через index.ts  
    Также `eslint` конфиг следит за тем, что бы всё импортировалось только из index.ts

    **Пример**:

    ```ts
    //entities/User/index.ts

    export type { User } from "./model/types/UserSchema.ts"

    export { UserCard } from "./ui/UserCard/UserCard.tsx"
    ```

    > ⚠️ **Предупреждение:** *Для файлов с тестами и storybook всё экспортируется через отдельный файл - testing.ts*

    Файл `testing.ts` должен лежать рядом с **index.ts**. Создавать стоит только там, откуда будут импорты

    **Пример**:

    ```ts
    //entities/User/testing.ts

    export type { User } from "./model/types/UserSchema.ts"

    export { UserCard } from "./ui/UserCard/UserCard.tsx"
    ```  

    Всё экспортируется точно так же, но из другого файла, отдельно для тестов




### Наименование файлов и компонентов

`React Кмпоненты` - С заглавной буквы (User, Profile, ...)   
`Typescript типы` - С заглавной буквы (ButtonVariants, UserProps, ProfileSchema, ...)    
`Хуки и утилиты` - С маленькой буквы (useDebounce, getDate, getIsString ...)    
`Слои` - С маленькой буквы (shared, entities, features, widgets, pages, app)    
`Сущности` - С заглавной буквы (UserAuth, ThemeSwitcher, Navbar, ...)   
`Подпапки сущностей` - С маленькой буквы (ui, model, types, slices, selectors, services, lib, api, hooks, ...)     
`Файл с типами` - С маленькой буквы (userShcema.ts, routerTypes.ts, ...)    
`Файлы для services и selectors` - С маленькой буквы (fetchUserById, fetchPublicUsers, getUserAuthData ...)    





## 👔 Eslint и Форматеры

Для форматирования кода используется 3 конфига - `eslint` | `stylelint` | `prettier`

1. [Prettier](./.prettierrc.js) - настроен в проекте, для форматирования ts кода  
2. [Styleliny](./.stylelintrc.cjs) - использует готовый собственный конфиг для форматирования *css* и *scss* файло  
3. [Eslint](./eslint.config.js) - использует готовый собственный конфиг с доролнительными опциями для форматирования кода  

Именно Eslint задаёт набор правил, отвечающих за прицнипы архитектуры проектов. О них было написано выше.  
Готовые конфиги для eslint и stylelint были взяты от сюда: - [Styleliny]([./.stylelintrc.cjs](https://www.npmjs.com/package/@meow-double/stylelint)) / [Eslint](https://www.npmjs.com/package/@meow-double/eslint)

**Команды:**  
 - `lint:ts` - запускает проверку ts и tsx файлов на соответствие eslint правилам
 - `lint:ts:fix` - запускает проверку ts и tsx файлов на соответствие правилам и устраняет возможные недостатки
 - `lint:scss` - запускает проверку css и scss файлов на соответствие stylelint правилам
 - `lint:scss:fix` - запускает проверку css и scss файлов на соответствие правилам и устраняет возможные недостатки
 - `format` - запускает проверку кода на соответствие prettier конфигу
 - `lint-staged` - используется для husky, в качестве проверки кода (подробнее в разделе CI/CD pipeline)



## 🧪 Тестирование
  Осуществляется для тестирование кода с целью выявление ошибок на этапе разработке  
  
  
  `Unit и Интеграционные тесты` - Vitets + Rtl  
  `Скриншотные тесты` - отсуствуют  
  `E2E тесты` - в процессе настраивания конфига (скорее всего cypress)

  Сам конфиг [vitest](./configs/vitest/vitest.config.ts) вместе с [rtl](./configs/vitest/setup.ts) настроен в папке configs/vitest

**Команды:**  
 - Что бы запустить тесты, вы можете использовать команду `npm run test:unit`  
 - Если вам необходимы отчёты тестов, вы также можете запустить `test:reports` - это запустит в браузере страницу, содержащую текущие отчёты тестов 
  

## 🏛️ Build сборка на vite

Сама сборка использует [Vite](https://vite.dev/), о котором вы сможете почитать подробнее по ссылке.

**Настройка Vite конфига:**
 - **legacy** плагин - для работы с browserslinst файлом  
 - **circleDependency** плагин - для выявления цикловых зависимостей в проекте
 - **removeAttrsPlugin** кастомный плагин - для удаления заданых атрибутов в проекте (мы используем data-testid) 

  `alias` - настроены alias в проекте, все папки относительно @


**Команды:**  
 - `dev` - запускает проект в режиме разработки
 - `build` - запускает сборку проекта, паралельно сжимает файлы в `.gz` | `.br` формат (подробнее в CI/CD pipelines)
 - `analyzer` - запускает станицу аналитики проекта в браузере


## ✨ CI/CD piplines

### Husky (Pre-commit)

Используется для выявления ошибок и несоответствий кода до момента попадания на удалённый репозиторий.  
Код проверяется в следущем порядке: `lint-staged` -> `build` -> `test:unit` -> `storybook:build`

Комиты делаются **понятными** и **читаемыми**   
**❌ Conventional Commits** не используется!


### Github Actions

*в процессе разработки...*

### Docker

*в процессе разработки...*

### Nginx

При конечой сборки **(build)** автоматически используется команда `npm run zip`, которая сжимает всю сборку в формат `gzip` и `brotli`.  
**Nginx** раздаёт файлы в этих форматах, либо чистые файлы (по надобности)

*в процессе разработки...*


## 📕 Storybook (самодокументация)

Используется storybook для документации компонентов.
*в процессе написания...*